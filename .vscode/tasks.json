{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/funstore.service.order/funstore.service.order.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}